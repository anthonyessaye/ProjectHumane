<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.themodernbit.projecthumane">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera2.full" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CameraActions.CameraActivity" />
        <activity android:name=".CameraActions.PicturePreviewActivity" />
        <activity android:name=".CapturedWordActivity" />
        <activity android:name=".TagsActivities.ChosenTranslationActivity" />

        <receiver
            android:name=".NotificationTaskerPackage.NotificationTasker"
            android:enabled="true"
            android:exported="false"></receiver>

        <service
            android:name=".NotificationTaskerPackage.NotificationTaskerService"
            android:exported="false"></service>

        <activity android:name=".SignUpActivity"></activity>
    </application>

</manifest>